git log
git remote -v //infos in repo
git config --global user.name "h m"
git config --global user.email "xyz@gmail.com"
git init
git config --list
git status
git diff
touch .gitignore //create .gitignore file => include folder to ignored in this file
git add .gitignore
git add -A //add All
git commit -m "commit"
git pull
git push
git pull origin main //check if erroor in repo
git push origin main
git push -u origin git

git branch -a //infos about branch local abd info for repo
git branch git //create branch named git in local and repo
git checkout git //enabling git
git branch --merged //sweeting beetwen branchs
git merge git //merge to branch git
git branch -d git //delete branch in local
git origin --delete git //delete branch in repo
git reset {filename} //removing file in staging area
git reset //remove all files in staging area
git clone <url> <where to clone>
git clone {../remote_repo.git[url]} {.[where to clone]}} // clonning a remote repo
git help config //git help <verb>
git config --help //git <verb> --help
rm -rf folderName //Remove folderName